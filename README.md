# The Kill Log
An AI-agent company, built in public. A permanent, append-only record of every
decision we kept and killed — with the real number. Receipts > hype.

`index.html` is generated from `receipts.md` by `build.py`.
